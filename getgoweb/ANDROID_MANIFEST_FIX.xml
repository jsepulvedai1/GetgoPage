<!-- 
    🔧 CORRECCIÓN PARA APP LINKS
    
    Cambios necesarios en AndroidManifest.xml:
    
    1. Agregar android:autoVerify="true" al intent-filter de getgo-page-h84g.vercel.app
    2. Opcional: Comentar los intent-filter sin autoVerify para evitar confusión
    3. El intent-filter con autoVerify debe estar activo (no comentado)
-->

<activity
    android:name=".MainActivity"
    android:exported="true"
    android:launchMode="singleTop"
    android:theme="@style/LaunchTheme"
    android:configChanges="orientation|screenSize|smallestScreenSize|keyboard|keyboardHidden|locale|layoutDirection|fontScale|density|uiMode"
    android:windowSoftInputMode="adjustResize">

    <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
    </intent-filter>
    
    <!-- 
        ✅ APP LINKS CON autoVerify (PRODUCCIÓN)
        Este es el que DEBE estar activo para que funcionen los App Links verificados
        Requiere que assetlinks.json esté configurado correctamente
    -->
    <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
            android:scheme="https"
            android:host="getgo-page-h84g.vercel.app"
            android:pathPrefix="/refer" />
    </intent-filter>

    <!-- 
        ⚠️ OPCIONAL: Intent-filters SIN autoVerify (para desarrollo/fallback)
        Estos funcionan pero mostrarán un diálogo para elegir navegador/app
        Puedes comentarlos si solo quieres usar App Links verificados
    -->
    <!--
    <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
            android:scheme="https"
            android:host="www.getgo.cl"
            android:pathPrefix="/refer" />
    </intent-filter>

    <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
            android:scheme="https"
            android:host="getgoapp.cl"
            android:pathPrefix="/refer" />
    </intent-filter>

    <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data
            android:scheme="https"
            android:host="getgoapp.com"
            android:pathPrefix="/refer" />
    </intent-filter>
    -->
    
    <meta-data
        android:name="io.flutter.embedding.android.SplashScreenDrawable"
        android:resource="@drawable/launch_background" />
</activity>



